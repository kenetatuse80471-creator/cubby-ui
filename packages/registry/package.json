{
  "name": "@cubby-ui/registry",
  "version": "0.0.0",
  "private": true,
  "description": "Source of the Cubby UI shadcn registry items: components, helpers and demos.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "registry:build": "pnpm -w run registry:build",
    "registry:validate": "pnpm -w run registry:validate"
  }
}
