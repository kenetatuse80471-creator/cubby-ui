import { TextArea } from "@/registry/cubby/ui/text-area";

/**
 * PLACEHOLDER — renders, but it is not yet a product example.
 * NEXT EXECUTOR: replace with one short, real use, the way `button/default.tsx` is written.
 */
export default function TextAreaDefault() {
  return <TextArea aria-label="Comment" placeholder="Leave a comment" />;
}
