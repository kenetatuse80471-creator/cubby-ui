---
"@cubby-ui/registry": minor
---

Первая партия примитивов: `icon`, `button`, `icon-button`, `tag`, `divider`, `avatar`, `spinner`,
`empty-state`, `switch` и вспомогательный элемент `cn`. Все значения — из `@cubby-ui/tokens`,
ни одного литерала цвета, размера или радиуса в коде компонентов.
